# CLAUDE.md — 行为准则

## 1. Token 与精简法则
- 所有 Markdown 文档：**极致精简、高信息密度、列表化**，拒绝长篇大论。
- 单文件 ≤80 行（含 `ARCHITECT.md`）。
- 代码注释只写"为什么"，不复述"做了什么"。
- 回答先结论后细节；不复述已知信息。

## 2. 读取指引
- 处理任何任务前：**先读 `ARCHITECT.md`** 建立全局认知。
- 只读与当前需求**严格相关**的代码文件，禁止全量漫游。
- `ARCHITECT.md` 与代码冲突时：以代码为准，并立即修正文档。
- 项目背景/部署/提交规范：见 `.claude/CLAUDE.md`。

## 3. 双向同步规则
- 架构诊断与重构计划 → **必须写入 `REFACTORING_TODO.md`**。
- 任何代码架构修改完成后 → **必须同步更新 `ARCHITECT.md`**（文件职责表 / 全局导出 / 逻辑流）。
- 完成重构 Task → 勾掉 `REFACTORING_TODO.md` 对应项。
- 文档更新与代码改动在**同一次 commit**，不分离。

## 4. 提交纪律
- 每次提交只包含**一个核心功能**，禁止多功能混合。
- 代码改动与其同步文档（ARCHITECT.md / REFACTORING_TODO.md）放**同一次提交**。
- 改 JS 必须在同提交内将 index.html `?v=N` +1。
- 提交信息：一行主题 + 列表说明修改内容。

## 5. 文档地图
| 文件 | 内容 | 行数上限 |
|---|---|---|
| `ARCHITECT.md` | 架构字典（分工/架构模式/逻辑流/数据流） | 80 |
| `REFACTORING_TODO.md` | 缺陷诊断 + 分阶段重构清单 | 80 |
| `.claude/CLAUDE.md` | 项目背景 / 部署 / 开发规范 | — |
